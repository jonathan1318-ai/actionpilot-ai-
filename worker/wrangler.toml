name = "actionpilot-gemini-proxy"
main = "src/index.ts"
compatibility_date = "2024-11-01"

[vars]
ALLOWED_ORIGIN = "http://localhost:5173"

# Set the real secret with:
#   npx wrangler secret put GEMINI_API_KEY
# Update ALLOWED_ORIGIN to your deployed app's origin before going to production.
